{
  "name": "forwardnotes",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "check": "node --check site/inspo/inspo.js",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
